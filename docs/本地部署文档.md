# AI 素材平台 - 本地部署文档

## 目录

1. [部署概述](#1-部署概述)
2. [前置准备](#2-前置准备)
3. [Docker Compose 部署（推荐）](#3-docker-compose-部署推荐)
4. [本地开发环境部署](#4-本地开发环境部�?
5. [部署验证](#5-部署验证)
6. [常见问题](#6-常见问题)

---

## 1. 部署概述

本文档介绍如何在本地环境部署 AI 素材平台。提供两种方式：

- **Docker Compose 部署**：一键启动所有服务，适合快速体验和测试
- **本地开发环境部�?*：适合开发调�?
### 1.1 本地部署架构

```
┌─────────────────────────────────────────────────�?�?             浏览�?(localhost:5173)            �?└──────────────────────┬──────────────────────────�?                       �?                       �?┌─────────────────────────────────────────────────�?�?           前端 (localhost:5173)                �?└──────────────────────┬──────────────────────────�?                       �?                       �?┌─────────────────────────────────────────────────�?�?          后端 API (localhost:3000)              �?├─────────────────────────────────────────────────�?�? ┌─────────────────�? ┌─────────────────�?     �?�? �? PostgreSQL     �? �?   Redis        �?     �?�? �? (localhost:5432)�? �? (localhost:6379)�?   �?�? └─────────────────�? └─────────────────�?     �?└─────────────────────────────────────────────────�?```

---

## 2. 前置准备

### 2.1 必需条件

在开始部署之前，请确保您已安装：

- **Docker** �?**Docker Compose**（Docker Compose 部署必需�?- **Node.js** 20+（本地开发必需�?- **pnpm**（包管理器）
- **Git**（可选，用于代码管理�?
### 2.2 可选准�?
- **AI 服务 API Keys**�?  - 通义万相 API Key
  - 豆包 API Key
  - Remove.bg API Key（可选）

---

## 3. Docker Compose 部署（推荐）

### 3.1 准备环境变量

1. 复制环境变量模板�?
```bash
cp .env.docker .env
```

2. 编辑 `.env` 文件，配置您�?API Keys�?
```env
# AI API 配置
TONGYI_API_KEY=sk-your-actual-api-key-here
DOUBAI_API_KEY=your-doubao-api-key
REMOVE_BG_API_KEY=your-remove-bg-api-key

# JWT 密钥（建议修改为随机字符串）
JWT_SECRET=your-strong-secret-key-here-change-in-production
```

### 3.2 启动服务

在项目根目录运行�?
```bash
docker-compose up -d
```

这将启动以下服务�?- **PostgreSQL** 数据库（端口 5432�?- **Redis** 缓存（端�?6379�?- **后端 API** 服务（端�?3000�?- **前端** 应用（端�?5173�?
### 3.3 查看服务状�?
```bash
# 查看所有服务状�?docker-compose ps

# 查看日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f server
docker-compose logs -f frontend
```

### 3.4 停止服务

```bash
# 停止但保留数�?docker-compose down

# 停止并删除所有数据（包括数据库）
docker-compose down -v
```

---

## 4. 本地开发环境部�?
### 4.1 后端服务部署

#### 4.1.1 进入后端目录

```bash
cd server
```

#### 4.1.2 安装依赖

```bash
pnpm install
```

#### 4.1.3 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env` 文件，配置数据库连接等：

```env
PORT=3000
NODE_ENV=development

# 数据库配置（需要本地运�?PostgreSQL�?DATABASE_URL=postgresql://postgres:postgres@localhost:5432/web-db
DATABASE_SSL=false

# Redis 配置（需要本地运�?Redis�?REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT 配置
JWT_SECRET=your-dev-secret-key
JWT_EXPIRES_IN=7d

# AI API 配置
TONGYI_API_KEY=your-tongyi-api-key
DOUBAI_API_KEY=your-doubao-api-key
REMOVE_BG_API_KEY=your-remove-bg-api-key

# 日志配置
LOG_LEVEL=debug
```

#### 4.1.4 启动数据库（可选）

如果使用 Docker Compose 只启动数据库�?
```bash
# 回到根目�?cd ..

# 只启动数据库和Redis
docker-compose up -d postgres redis
```

#### 4.1.5 启动后端服务

```bash
cd server

# 开发模式（支持热重载）
pnpm start:dev

# 或生产模�?pnpm build
pnpm start:prod
```

后端服务将在 `http://localhost:3000` 启动�?
### 4.2 前端服务部署

#### 4.2.1 进入前端目录

```bash
cd web
```

#### 4.2.2 安装依赖

```bash
pnpm install
```

#### 4.2.3 配置 API 地址

编辑 `web/src/services/api/axios.instance.ts` 或创�?`.env` 文件�?
```env
VITE_API_URL=http://localhost:3000/api
```

#### 4.2.4 启动前端服务

```bash
# 开发模�?pnpm dev

# 或构建并预览
pnpm build
pnpm preview
```

前端应用将在 `http://localhost:5173` 启动�?
---

## 5. 部署验证

### 5.1 Docker Compose 部署验证

#### 5.1.1 检查服务状�?
```bash
docker-compose ps
```

所有服务状态应�?`Up`�?
#### 5.1.2 访问应用

- **前端**：打开浏览器访�?`http://localhost:5173`
- **后端 API**：访�?`http://localhost:3000`（如果有健康检查端点）
- **数据�?*：使�?PostgreSQL 客户端连�?`localhost:5432`
- **Redis**：使�?Redis 客户端连�?`localhost:6379`

### 5.2 本地开发部署验�?
#### 5.2.1 验证后端

```bash
# 访问 Swagger 文档
http://localhost:3000/api/docs
```

**注意**：后�?API 路由已统一添加 `/api` 前缀，例如：
- 用户登录：`POST /api/user/login`
- AI 生成：`POST /api/ai/generate`
- 素材上传：`POST /api/material/upload`

#### 5.2.2 验证前端

打开浏览器访�?`http://localhost:5173`，检查：
- 页面正常加载
- 控制台无错误
- API 请求正常

### 5.3 功能测试

1. **注册/登录**：测试用户认�?2. **AI 生成**：测试图片生成功�?3. **素材�?*：测试素材上传和管理
4. **抠图功能**：测试背景移�?
---

## 6. 常见问题

### 6.1 Docker Compose 部署问题

#### 问题：端口被占用

**解决方案**�?
修改 `docker-compose.yml` 中的端口映射，或停止占用端口的服务：

```bash
# 查看端口占用
netstat -ano | findstr :3000  # Windows
lsof -i :3000                # macOS/Linux

# 停止占用端口的进�?taskkill /PID <进程ID> /F    # Windows
kill -9 <进程ID>             # macOS/Linux
```

#### 问题：服务启动失�?
**解决方案**�?
1. 查看日志�?```bash
docker-compose logs -f
```

2. 检查环境变量配置是否正�?3. 确保 Docker 有足够的内存（建议至�?4GB�?
#### 问题：数据库连接失败

**解决方案**�?
1. 确认 PostgreSQL 服务已启�?2. 检�?`DATABASE_URL` 配置
3. 查看后端服务日志�?```bash
docker-compose logs server
```

### 6.2 本地开发问�?
#### 问题：pnpm install 失败

**解决方案**�?
```bash
# 清理缓存
pnpm store prune

# 重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

#### 问题：后端启动失�?
**解决方案**�?
1. 确认 PostgreSQL �?Redis 正在运行
2. 检�?`.env` 配置
3. 查看错误日志
4. 确保 Node.js 版本 >= 20

#### 问题：前�?API 请求失败

**解决方案**�?
1. 确认后端服务正在运行
2. 检�?`VITE_API_URL` 配置
3. 检查浏览器控制台网络请�?4. 确认没有 CORS 问题

### 6.3 数据持久�?
Docker Compose 部署中，数据通过 volume 持久化：

- **PostgreSQL 数据**：存储在 `postgres_data` volume
- **Redis 数据**：存储在 `redis_data` volume

删除 volume 会清空数据：

```bash
# 停止并删除所有数�?docker-compose down -v
```

### 6.4 性能优化

#### 本地开发优�?
- 使用 `pnpm start:dev` 启用热重�?- 启用 TypeScript 严格模式检�?- 使用 ESLint �?Prettier 保持代码质量

#### Docker 优化

- 使用 `.dockerignore` 排除不必要的文件
- 多阶段构建减小镜像体�?- 合理配置资源限制

---

## 附录

### A. 快速参考命�?
```bash
# Docker Compose
docker-compose up -d          # 启动所有服�?docker-compose down           # 停止服务
docker-compose logs -f        # 查看日志
docker-compose ps             # 查看状�?
# 后端开�?cd server
pnpm install                  # 安装依赖
pnpm start:dev               # 开发模�?pnpm build                   # 构建
pnpm start:prod              # 生产模式

# 前端开�?cd web
pnpm install                  # 安装依赖
pnpm dev                     # 开发模�?pnpm build                   # 构建
pnpm preview                 # 预览构建
```

### B. 相关资源

- [Docker 官方文档](https://docs.docker.com)
- [Docker Compose 文档](https://docs.docker.com/compose)
- [Node.js 官方文档](https://nodejs.org/docs)
- [pnpm 文档](https://pnpm.io)
- [NestJS 文档](https://docs.nestjs.com)
- [Vite 文档](https://vitejs.dev)
