# AI 素材平台 - 前端部署文档

## 目录

1. [部署概述](#1-部署概述)
2. [前置准备](#2-前置准备)
3. [Railway 部署步骤](#3-railway-部署步骤)
4. [环境变量配置](#4-环境变量配置)
5. [自定义域名配置](#5-自定义域名配�?
6. [部署验证](#6-部署验证)
7. [常见问题](#7-常见问题)

---

## 1. 部署概述

本文档详细介绍如何使�?Railway 免费部署 AI 素材平台前端。Railway 提供以下优势�?
- **免费额度**：适合个人项目和小型应�?- **自动部署**：每次推送到 GitHub 自动触发部署
- **全球 CDN**：快速的内容分发网络
- **HTTPS 支持**：自动配�?SSL 证书
- **预览功能**：每�?Pull Request 生成预览链接

### 1.1 部署架构

```
┌─────────────────�?�?  浏览�?       �?└────────┬────────�?         �?         �?┌─────────────────�?�? 前端 (Railway) �?└────────┬────────�?         �?         �?┌─────────────────�?�? 后端 (Railway) �?└─────────────────�?```

---

## 2. 前置准备

### 2.1 必需条件

在开始部署之前，请确保您拥有�?
- **GitHub 账号**：用于托管代码仓�?- **Railway 账号**：可以使�?GitHub 账号直接登录
- **已推送的代码仓库**：代码已推送到 GitHub
- **后端服务已部�?*：获取后�?API 地址

### 2.2 仓库结构要求

确保您的仓库结构符合以下要求�?
```
web-platform/          # 根目录（仓库根目录）
├── web/               # 前端项目目录
�?  ├── src/
�?  ├── package.json
�?  ├── railway.json
�?  └── ...
├── server/                    # 后端项目目录
└── docs/
```

### 2.3 本地构建测试

在部署前，建议先在本地测试构建是否正常：

```bash
cd web
pnpm install
pnpm build

# 检查是否成功生�?dist 目录
```

---

## 3. Railway 部署步骤

### 3.1 步骤一：登�?Railway

1. 访问 [Railway 官网](https://railway.app)
2. 点击右上�?**Login**
3. 选择 **Login with GitHub** 使用 GitHub 账号登录
4. 完成 Railway 账号创建和登�?
### 3.2 步骤二：创建新项�?
1. 登录后点�?**New Project**
2. 选择 **Deploy from GitHub repo**
3. 如果是第一次使用，可能需要授�?Railway 访问您的 GitHub 仓库
4. 搜索并选择您的仓库

### 3.3 步骤三：配置前端服务

#### 3.3.1 基本配置

- **Service Name**：`web-frontend`（或自定义）
- **Root Directory**：`web`
- **Branch**：选择您的主分支（�?`main` �?`master`�?
#### 3.3.2 构建和启动命�?
Railway �?Nixpacks 会自动检�?Vite 项目，通常不需要额外配置。但可以确认�?
- **Build Command**：`pnpm install && pnpm build`
- **Start Command**：`pnpm preview`（或使用 nginx 托管静态文件）

#### 3.3.3 环境变量

�?**Variables** 部分添加�?
| 变量�?| �?| 说明 |
|--------|-----|------|
| `VITE_API_URL` | `https://your-backend-url.railway.app/api` | 后端 API 地址（注意需要添�?`/api` 前缀�?|

先点�?**Deploy** 部署一次，等会儿再配置环境变量�?
### 3.4 步骤四：部署成功

部署成功后，Railway 会生成一个域名，类似�?`https://web-frontend-production.up.railway.app`

您可以在服务设置中自定义域名�?
---

## 4. 环境变量配置

### 4.1 �?Railway 项目设置中配�?
1. 进入 Railway 前端服务页面
2. 点击 **Variables** 标签
3. 点击 **New Variable** 添加环境变量

### 4.2 完整的环境变量列�?
| 变量�?| 是否必需 | 示例�?| 说明 |
|--------|---------|--------|------|
| `VITE_API_URL` | �?| `https://web-backend.up.railway.app/api` | 后端 API 地址（注意需要添�?`/api` 前缀�?|

### 4.3 更新环境变量后的重部�?
修改环境变量�?Railway 会自动重新部署�?
---

## 5. 自定义域名配�?
### 5.1 步骤一：添加自定义域名

1. 进入 Railway 前端服务页面
2. 点击 **Settings** �?**Domains**
3. 点击 **+ New Domain**
4. 输入您的域名，如 `app.yourdomain.com`
5. 点击 **Add Domain**

### 5.2 步骤二：配置 DNS

Railway 会提�?DNS 配置信息�?
1. 登录您的域名注册商（�?Cloudflare、阿里云等）
2. 添加 CNAME 记录，指�?Railway 提供的域�?3. 等待 DNS 生效（可能需要几分钟到几小时�?
### 5.3 步骤三：验证域名配置

Railway 会自动验证域名配置并颁发 HTTPS 证书�?
---

## 6. 部署验证

### 6.1 基本功能验证

部署完成后，请验证以下功能：

1. **访问首页**：打开部署后的链接，确认页面正常加�?2. **查看控制�?*：检查浏览器开发者工具的 Console 标签页是否有错误
3. **检查网络请�?*：查�?Network 标签页，确认 API 请求�?URL 正确
4. **页面路由**：测试各页面路由跳转是否正常

### 6.2 完整流程验证

如果后端也已部署，完成以下测试：

1. **用户注册/登录**：测试认证流�?2. **AI 生成**：测试图片生成功�?3. **素材上传**：测试素材上传和管理
4. **任务状�?*：测试任务状态轮询和更新

### 6.3 性能检�?
使用以下工具检查性能�?
- [Lighthouse](https://developer.chrome.com/docs/lighthouse/overview/)：Chrome 开发者工具内�?- [WebPageTest](https://www.webpagetest.org/)：测试全球各地区访问速度
- [GTmetrix](https://gtmetrix.com/)：性能分析和优化建�?
---

## 7. 常见问题

### 7.1 部署失败常见原因

#### 问题：构建失败，提示找不到模�?
**解决方案**�?- 检�?`package.json` �?dependencies 是否包含所有必需�?- 确认 `node_modules` 已添加到 `.gitignore`
- 检查构建命令是否正�?- 查看 Railway 部署日志

#### 问题：部署成功但页面空白或报�?
**解决方案**�?- 检�?`VITE_API_URL` 环境变量是否正确配置（需要包�?`/api` 前缀�?- 检查浏览器控制台的错误信息
- 确认后端 API 可正常访�?- 检�?CORS 配置

#### 问题：刷新页面显�?404 错误

**解决方案**�?
需要配�?SPA 路由重定向。在 `web` 目录创建 `static.json`�?
```json
{
  "root": "dist/",
  "clean_urls": true,
  "routes": {
    "/**": "index.html"
  }
}
```

或者使�?Nginx 配置�?
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

### 7.2 环境变量相关

#### 问题：环境变量未生效

**解决方案**�?- 确认环境变量已正确添加到 Railway 服务设置
- 环境变量名必须以 `VITE_` 开头才能在客户端访�?- 等待 Railway 自动重新部署

### 7.3 部署�?CI/CD

#### 问题：如何自动部�?
**解决方案**�?Railway 默认配置为推送到指定分支自动部署。可以在服务设置 �?**GitHub** 中调整这些设置�?
#### 问题：如何查看部署日�?
**解决方案**�?1. 进入服务页面
2. 点击 **Deployments** 标签
3. 选择要查看的部署记录
4. 点击 **View Logs** 查看详细日志

### 7.4 性能和优�?
#### 问题：如何优化部署性能

**解决方案**�?- 确保 Vite 配置正确
- 使用路由懒加载（React.lazy�?- 图片压缩和优�?- 启用 Railway 内置的优化功�?
#### 问题：图片加载缓�?
**解决方案**�?- 使用图片 CDN（如 Cloudinary、阿里云 OSS 等）
- 启用图片懒加�?- 使用 WebP 等现代图片格�?- 配置合理的缓存策�?
### 7.5 其他问题

#### 问题：部署后如何回滚到旧版本

**解决方案**�?1. �?**Deployments** 页面找到要回滚的版本
2. 点击右侧三个�?3. 选择 **Redeploy** 重新部署该版�?
#### 问题：如何删除服�?
**解决方案**�?1. 进入服务设置页面
2. 滚动到底�?3. 点击 **Delete Service**
4. 确认删除

---

## 附录

### A. Railway 免费额度限制

| 资源 | 免费额度 |
|------|---------|
| 执行时长 | 500 小时/�?|
| 出站带宽 | 100GB/�?|
| 项目数量 | 无限 |
| 服务数量 | 无限 |

**注意**：当执行时长用完时，服务会自动暂停。下个月自动重置�?
### B. 相关资源链接

- [Railway 官方文档](https://docs.railway.app)
- [Railway 部署文档](https://docs.railway.app/deploy/deployments)
- [Vite 部署文档](https://cn.vitejs.dev/guide/build.html)
- [React Router 部署指南](https://reactrouter.com/en/main/start/tutorial#deploying)

### C. 快速参考命�?
```bash
# 本地测试构建
cd web
pnpm build

# 预览构建结果
pnpm preview
```
