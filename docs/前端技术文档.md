# AI 素材平台 - 前端技术文�?
## 目录

1. [项目概述](#1-项目概述)
2. [技术栈详解](#2-技术栈详解)
3. [架构设计](#3-架构设计)
4. [核心功能模块](#4-核心功能模块)
5. [组件目录结构](#5-组件目录结构)
6. [状态管理](#6-状态管�?
7. [路由配置](#7-路由配置)
8. [API 服务封装](#8-api-服务封装)
9. [开发规范](#9-开发规�?
10. [性能优化](#10-性能优化)

---

## 1. 项目概述

AI 素材平台前端是一个现代化�?Web 应用，提�?AI 图片生成、抠图、批量处理、素材库管理等功能�?
### 1.1 核心功能

- **用户认证**：注册、登录、Token 管理
- **AI 图片生成**：基于提示词生成图片
- **图片编辑**：基于提示词编辑图片
- **图片抠图**：自动去除图片背�?- **批量处理**：批�?AI 任务处理
- **素材�?*：图片素材的上传、查看、编辑、删�?- **任务状态管�?*：AI 任务状态实时跟�?
---

## 2. 技术栈详解

| 技�?| 版本 | 用�?| 选型理由 |
|-----|------|------|---------|
| React | 19.2.5 | UI 框架 | 生态丰富，社区活跃，组件化开�?|
| TypeScript | 6.0.2 | 类型安全 | 提供静态类型检查，减少运行时错�?|
| Vite | 8.0.9 | 构建工具 | 开发体验好，构建速度快，支持 HMR |
| React Router DOM | 7.14.1 | 路由管理 | 官方推荐，支持嵌套路由和懒加�?|
| Zustand | 5.0.12 | 状态管�?| 轻量级，API 简洁，学习成本低，支持持久�?|
| Tailwind CSS | 4.2.3 | 样式框架 | 原子�?CSS，开发效率高，构建体积小 |
| shadcn/ui | 4.3.1 | UI 组件�?| 可定制性强，与 Tailwind 完美配合 |
| Axios | 1.15.1 | HTTP 客户�?| 支持拦截器，功能完善，便于统一错误处理 |
| Lucide React | 1.8.0 | 图标�?| 现代化、美观的图标组件 |
| Vitest | 3.0.7 | 测试框架 | Vite 原生支持，测试速度�?|

### 2.1 关键依赖介绍

#### React 19
- 支持 React Compiler 自动优化渲染
- 新的 Hooks 和组�?API
- 更好�?TypeScript 支持

#### Zustand 5
- 极简的状态管理库
- 支持持久化中间件
- 避免不必要的重渲�?- 简单的订阅机制

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────�?�?                 页面�?(Pages)                           �?�? HomePage | GeneratePage | LibraryPage | ...     �?└─────────────────────────────────────────────────────�?                        �?                        �?┌─────────────────────────────────────────────────────�?�?               组件�?(Components)                    �?�? Layout | Navbar | Sidebar | UI Components         �?└─────────────────────────────────────────────────────�?                        �?                        �?┌─────────────────────────────────────────────────────�?�?              业务逻辑�?(Hooks & Services)            �?�? useAIGeneration | useTaskPolling | API Service �?└─────────────────────────────────────────────────────�?                        �?                        �?┌─────────────────────────────────────────────────────�?�?                状态层 (Store)                         �?�? useUserStore | useTaskStore                      �?└─────────────────────────────────────────────────────�?                        �?                        �?┌─────────────────────────────────────────────────────�?�?                工具�?(Utils & Lib)                     �?�? utils.ts | constants.ts                            �?└─────────────────────────────────────────────────────�?```

### 3.2 设计原则

- **组件�?*：每个功能拆分为独立、可复用的组�?- **单一职责**：每个组�?函数只负责一件事
- **类型安全**：全程使�?TypeScript
- **状态隔�?*：使�?Zustand 管理共享状�?- **可维护�?*：清晰的目录结构和注�?
---

## 4. 核心功能模块

### 4.1 用户认证模块

- **登录/注册**：用户凭证验�?- **Token 管理**：JWT Token 的存储和自动附加
- **路由守卫**：ProtectedRoute 组件保护需要登录的页面
- **用户状�?*：用户信息、积分等状态管�?
### 4.2 AI 生成模块

- **提示词输�?*：支持提示词模板和自定义输入
- **参数配置**：模型选择、尺寸、数量等
- **任务提交**：异步提�?AI 生成任务
- **进度跟踪**：轮询获取任务进度和状�?- **结果展示**：生成的图片展示和下�?
### 4.3 素材库模�?
- **素材上传**：支持多种图片格�?- **素材列表**：分页展示素�?- **素材编辑**：重命名、分类等
- **素材删除**：安全删除确�?
---

## 5. 组件目录结构

```
web/src/
├── components/
�?  ├── auth/
�?  �?  └── ProtectedRoute.tsx      # 路由守卫组件
�?  ├── editor/
�?  �?  └── ImageEditor.tsx        # 图片编辑器组�?�?  ├── layout/
�?  �?  ├── Layout.tsx            # 整体布局组件
�?  �?  ├── Navbar.tsx           # 顶部导航�?�?  �?  └── Sidebar.tsx          # 侧边栏导�?�?  └── ui/
�?      ├── EmptyState.tsx       # 空状态组�?�?      ├── LoadingSpinner.tsx     # 加载动画
�?      ├── PageLoading.tsx      # 页面加载
�?      ├── Skeleton.tsx           # 骨架�?�?      ├── Toast.tsx              # 消息提示
�?      ├── button.tsx             # 按钮组件
�?      ├── card.tsx           # 卡片组件
�?      ├── dialog.tsx            # 对话框组�?�?      ├── input.tsx            # 输入框组�?�?      ├── progress.tsx           # 进度条组�?�?      ├── select.tsx             # 选择器组�?�?      ├── slider.tsx             # 滑块组件
�?      ├── tabs.tsx               # 标签页组�?�?      └── textarea.tsx           # 文本域组�?├── hooks/
�?  ├── useAIGeneration.ts        # AI 生成 Hook
�?  ├── useDebounce.ts           # 防抖 Hook
�?  ├── useFileUpload.ts         # 文件上传 Hook
�?  └── useTaskPolling.ts        # 任务轮询 Hook
├── pages/
�?  ├── BatchPage.tsx            # 批量处理�?�?  ├── GeneratePage.tsx         # AI 生成�?�?  ├── HomePage.tsx            # 首页
�?  ├── LibraryPage.tsx         # 素材库页
�?  ├── LoginPage.tsx           # 登录�?�?  ├── RemoveBgPage.tsx          # 抠图�?�?  └── StatusPage.tsx           # 任务状态页
├── services/
�?  └── api/
�?      ├── ai.api.ts            # AI 相关 API
�?      ├── axios.instance.ts    # Axios 实例配置
�?      ├── material.api.ts       # 素材相关 API
�?      └── user.api.ts          # 用户相关 API
├── store/
�?  ├── task.store.ts             # 任务状态管�?�?  └── user.store.ts            # 用户状态管�?├── constants/
�?  └── prompt-templates.ts       # 提示词模�?├── lib/
�?  └── utils.ts                # 工具函数
├── styles/
�?  └── globals.css             # 全局样式
├── App.tsx                    # 根组�?└── main.tsx                  # 应用入口
```

---

## 6. 状态管�?
项目使用 **Zustand** 进行状态管理，有两个主要的 Store�?
### 6.1 User Store (用户状�?

**文件位置**: `store/user.store.ts`

管理用户信息、Token、登出等状态�?
```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface User {
  id: number;
  email: string;
  credits: number;
}

interface UserStore {
  user: User | null;
  token: string | null;
  setUser: (user: User | null) => void;
  setToken: (token: string | null) => void;
  logout: () => void;
}

export const useUserStore = create<UserStore>()(
  persist(
    (set) => ({
      user: null,
      token: null,
      setUser: (user) => set({ user }),
      setToken: (token) => set({ token }),
      logout: () => set({ user: null, token: null }),
    }),
    {
      name: 'user-storage', // localStorage key
    }
  )
);
```

**持久�?*: 使用 `persist` 中间件，自动保存�?localStorage�?
### 6.2 Task Store (任务状�?

**文件位置**: `store/task.store.ts`

管理 AI 任务的状态管理�?
### 6.3 使用示例

```tsx
import { useUserStore } from '@/store/user.store';

function Header() {
  const { user, logout } = useUserStore();
  
  return (
    <div>
      <span>欢迎，{user?.email}</span>
      <button onClick={logout}>退�?/button>
    </div>
  );
}
```

---

## 7. 路由配置

### 7.1 路由结构

路由�?`App.tsx` 中定义，使用 React Router v7�?
```
/                       # 首页（公开�?/login                  # 登录页（公开�?/generate              # AI 生成页（需登录�?/library               # 素材库（需登录�?/remove-bg             # 抠图页（需登录�?/batch                 # 批量处理页（需登录�?/status/:id             # 任务状态页（需登录�?```

### 7.2 ProtectedRoute (路由守卫)

**文件位置**: `components/auth/ProtectedRoute.tsx`

检查用户是否登录，未登录则重定向到登录页�?
```tsx
import { Navigate, useLocation } from 'react-router-dom';
import { useUserStore } from '@/store/user.store';

export function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const { token } = useUserStore();
  const location = useLocation();

  if (!token) {
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  return <>{children}</>;
}
```

### 7.3 路由配置示例

```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { ProtectedRoute } from '@/components/auth/ProtectedRoute';
import { Layout } from '@/components/layout/Layout';
import { HomePage } from '@/pages/HomePage';
import { LoginPage } from '@/pages/LoginPage';
import { GeneratePage } from '@/pages/GeneratePage';
import { LibraryPage } from '@/pages/LibraryPage';
import { RemoveBgPage } from '@/pages/RemoveBgPage';
import { BatchPage } from '@/pages/BatchPage';
import { StatusPage } from '@/pages/StatusPage';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Layout />}>
          <Route index element={<HomePage />} />
          <Route path="login" element={<LoginPage />} />
          <Route path="generate" element={
            <ProtectedRoute><GeneratePage /></ProtectedRoute>
          } />
          <Route path="library" element={
            <ProtectedRoute><LibraryPage /></ProtectedRoute>
          } />
          <Route path="remove-bg" element={
            <ProtectedRoute><RemoveBgPage /></ProtectedRoute>
          } />
          <Route path="batch" element={
            <ProtectedRoute><BatchPage /></ProtectedRoute>
          } />
          <Route path="status/:id" element={
            <ProtectedRoute><StatusPage /></ProtectedRoute>
          } />
        </Route>
      </Routes>
    </BrowserRouter>
  );
}
```

---

## 8. API 服务封装

### 8.1 Axios 实例配置

**文件位置**: `services/api/axios.instance.ts`

配置统一�?Axios 实例，包括基础 URL、拦截器等�?
```typescript
import axios from 'axios';
import { useUserStore } from '@/store/user.store';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:3000',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器：自动添加 Authorization header
api.interceptors.request.use(
  (config) => {
    const token = useUserStore.getState().token;
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器：解包响应格式，统一错误处理
api.interceptors.response.use(
  (response) => {
    const { code, data, message } = response.data;
    if (code === 200) {
      return data;
    }
    throw new Error(message || '请求失败');
  },
  (error) => {
    const message = error.response?.data?.message || error.message || '网络错误';
    // 这里可以统一 Toast 提示错误
    return Promise.reject(new Error(message));
  }
);

export default api;
```

### 8.2 API 服务模块

#### AI API (`services/api/ai.api.ts`

```typescript
import api from './axios.instance';

export interface GenerateImageParams {
  prompt: string;
  model?: string;
  size?: string;
  count?: number;
}

export interface AITask {
  id: number;
  type: string;
  status: 'pending' | 'processing' | 'completed' | 'failed';
  progress: number;
  result?: any;
  error?: string;
}

export const aiApi = {
  generate: (params: GenerateImageParams): Promise<{ taskId: number }> => {
    return api.post('/ai/generate', params);
  },

  removeBg: (file: File): Promise<{ taskId: number }> => {
    const formData = new FormData();
    formData.append('file', file);
    return api.post('/ai/remove-bg', formData, {
      headers: { 'Content-Type': 'multipart/form-data' },
    });
  },

  getTask: (id: number): Promise<AITask> => {
    return api.get(`/ai/tasks/${id}`);
  },

  getTasks: (): Promise<AITask[]> => {
    return api.get('/ai/tasks');
  },
};
```

#### 素材 API (`services/api/material.api.ts`

```typescript
import api from './axios.instance';

export interface Material {
  id: number;
  name: string;
  url: string;
  size: number;
  type: string;
  category?: string;
  createdAt: string;
}

export const materialApi = {
  list: (): Promise<Material[]> => {
    return api.get('/material');
  },

  upload: (file: File): Promise<Material> => {
    const formData = new FormData();
    formData.append('file', file);
    return api.post('/material/upload', formData, {
      headers: { 'Content-Type': 'multipart/form-data' },
    });
  },

  update: (id: number, data: Partial<Material>): Promise<Material> => {
    return api.put(`/material/${id}`, data);
  },

  delete: (id: number): Promise<void> => {
    return api.delete(`/material/${id}`);
  },
};
```

#### 用户 API (`services/api/user.api.ts`

```typescript
import api from './axios.instance';

export interface User {
  id: number;
  email: string;
  credits: number;
}

export interface LoginParams {
  email: string;
  password: string;
}

export interface RegisterParams {
  email: string;
  password: string;
}

export const userApi = {
  login: (params: LoginParams): Promise<{ token: string; user: User }> => {
    return api.post('/auth/login', params);
  },

  register: (params: RegisterParams): Promise<{ token: string; user: User }> => {
    return api.post('/auth/register', params);
  },

  getProfile: (): Promise<User> => {
    return api.get('/user/profile');
  },
};
```

---

## 9. 开发规�?
### 9.1 代码风格

- 使用 TypeScript 严格模式
- 使用 `const` 优先，少�?`let`，不�?`var`
- 使用箭头函数
- 使用 `async/await` 处理异步

### 9.2 组件编写规范

#### 组件文件命名

- 组件文件使用 PascalCase，如 `UserProfile.tsx`
- 组件函数名与文件名一�?- 导出默认组件

#### 组件 Props 定义

```tsx
interface ButtonProps {
  children: React.ReactNode;
  onClick?: () => void;
  variant?: 'primary' | 'secondary';
  disabled?: boolean;
}

export function Button({ 
  children, 
  onClick, 
  variant = 'primary',
  disabled = false 
}: ButtonProps) {
  return (
    <button 
      onClick={onClick} 
      disabled={disabled}
      className={cn(
        'px-4 py-2 rounded',
        variant === 'primary' ? 'bg-blue-500 text-white' : 'bg-gray-200'
      )}
    >
      {children}
    </button>
  );
}
```

### 9.3 Git 提交规范

- `feat:` 新功�?- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 重构
- `test:` 测试相关
- `chore:` 构建/工具相关

---

## 10. 性能优化

### 10.1 前端优化策略

#### 路由懒加�?
使用 React.lazy �?Suspense 减少首屏加载体积�?
```tsx
import { lazy, Suspense } from 'react';

const GeneratePage = lazy(() => import('@/pages/GeneratePage'));
const LibraryPage = lazy(() => import('@/pages/LibraryPage'));

// 使用�?<Suspense fallback={<PageLoading />}>
  <GeneratePage />
</Suspense>
```

#### 状态管理优�?
Zustand 按需订阅，避免不必要的重渲染�?
```tsx
// 只订阅需要的部分
const user = useUserStore(state => state.user);
const token = useUserStore(state => state.token);
```

#### 图片优化

- 使用 WebP 格式
- 适当压缩图片
- 使用图片懒加�?
### 10.2 构建优化

Vite 配置优化 (`vite.config.ts`)

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import path from 'path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          ui: ['lucide-react'],
        },
      },
    },
  },
});
```

---

## 附录

### A. 本地开发环境搭�?
```bash
# 安装依赖
cd web
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览生产构建
pnpm preview

# 运行测试
pnpm test
```

### B. 环境变量

在项目根目录创建 `.env` 文件�?
```env
VITE_API_URL=http://localhost:3000
```

生产环境配置�?Vercel 后台设置�?
### C. 相关资源链接

- [React 文档](https://react.dev)
- [TypeScript 文档](https://www.typescriptlang.org/docs)
- [Vite 文档](https://cn.vitejs.dev)
- [Tailwind CSS 文档](https://tailwindcss.com)
- [Zustand 文档](https://zustand.docs.pmnd.rs)
- [React Router 文档](https://reactrouter.com)
- [shadcn/ui 文档](https://ui.shadcn.com)
