# AI 素材平台 - 服务端技术文�?
## 目录

1. [项目概述](#1-项目概述)
2. [技术栈详解](#2-技术栈详解)
3. [架构设计](#3-架构设计)
4. [项目目录结构](#4-项目目录结构)
5. [核心模块详解](#5-核心模块详解)
6. [API 设计规范](#6-api-设计规范)
7. [数据库设计](#7-数据库设�?
8. [安全策略](#8-安全策略)
9. [异步任务处理](#9-异步任务处理)
10. [日志系统](#10-日志系统)
11. [开发规范](#11-开发规�?

---

## 1. 项目概述

AI 素材平台后端是一个企业级�?REST API 服务，提供用户认证、AI 任务处理、素材管理等功能。采用模块化架构，支持高可用和可扩展�?
### 1.1 核心功能

- **用户认证与授�?*：JWT Token 认证、权限管�?- **AI 任务处理**：图片生成、抠图、编辑、批量处�?- **素材管理**：文件上传、素�?CRUD
- **任务队列**：Bull + Redis 异步任务处理
- **缓存管理**：Redis 缓存热点数据

---

## 2. 技术栈详解

| 技�?| 版本 | 用�?| 选型理由 |
|-----|------|------|---------|
| NestJS | 11.0.1 | 后端框架 | 企业级、模块化、TypeScript 原生支持、依赖注�?|
| TypeScript | 5.7.3 | 类型安全 | 与前端统一语言、全栈类型共�?|
| TypeORM | 0.3.28 | ORM 框架 | 支持多种数据库、迁移工具完善、装饰器 API |
| PostgreSQL | 18 | 关系型数据库 | 功能强大、支�?JSONB、事务支持完�?|
| Redis | 7.x | 缓存/队列 | 高性能、支�?Bull 队列、数据持久化 |
| Bull | 4.16.5 | 任务队列 | 基于 Redis、支持任务调度和重试、UI 监控 |
| JWT | - | 认证授权 | 无状态认证、便于横向扩�?|
| Swagger | 11.3.0 | API 文档 | 自动生成文档、便于调试和集成 |
| Winston | 3.17.0 | 日志系统 | 功能强大、支持日志轮转、多传输 |
| bcrypt | 6.0.0 | 密码加密 | 安全的密码哈希算�?|
| class-validator | 0.15.1 | 参数验证 | 装饰器风格的 DTO 验证 |
| express-rate-limit | 8.3.2 | 接口限流 | 防止恶意请求、保护服�?|

### 2.1 关键技术介�?
#### NestJS 11
- 基于 Express 的企业级 Node.js 框架
- 模块化架构，支持依赖注入
- 完善的中间件、守卫、拦截器、管道机�?- 内置支持 TypeScript

#### Bull
- 基于 Redis 的任务队列库
- 支持任务优先级、延迟、重�?- 支持任务进度更新
- 提供监控 UI

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────�?�?                  请求�?(Controllers)              �?�? AuthController | UserController | AIController   �?└─────────────────────────────────────────────────────�?                        �?                        �?┌─────────────────────────────────────────────────────�?�?                 业务�?(Services)                   �?�? AuthService | AIService | MaterialService      �?└─────────────────────────────────────────────────────�?                        �?                        �?┌─────────────────────────────────────────────────────�?�?                 数据�?(Repositories)             �?�? TypeORM Repositories | Cache Service           �?└─────────────────────────────────────────────────────�?         �?                         �?         �?                         �?┌──────────────────�?     ┌──────────────────�?�?  PostgreSQL     �?     �?     Redis       �?�? - User Table   �?     �? - Cache        �?�? - Task Table   �?     �? - Bull Queue   �?�? - Material Tab �?     └──────────────────�?└──────────────────�?         �?         �?┌─────────────────────────────────────────────────────�?�?             第三�?AI 服务集成                       �?�? - 通义万相 (图片生成)                              �?�? - 豆包 (图片编辑)                                  �?�? - Remove.bg (抠图)                                 �?└─────────────────────────────────────────────────────�?```

### 3.2 核心组件

#### Guards (守卫)
- **JwtAuthGuard**: JWT Token 验证
- 用于保护需要认证的路由

#### Interceptors (拦截�?
- **TransformInterceptor**: 统一响应格式包装
- **LoggingInterceptor**: 请求日志记录

#### Filters (过滤�?
- **HttpExceptionFilter**: 全局异常处理

#### Pipes (管道)
- **ValidationPipe**: DTO 参数验证

---

## 4. 项目目录结构

```
server/src/
├── ai/                                    # AI 模块
�?  ├── constants/
�?  �?  ├── models.ts                       # AI 模型配置
�?  �?  └── prompt-templates.ts            # 提示词模�?�?  ├── controllers/
�?  �?  └── ai-model.controller.ts        # AI 模型控制�?�?  ├── dto/
�?  �?  ├── ai-model.dto.ts               # 模型 DTO
�?  �?  ├── batch-task.dto.ts            # 批量任务 DTO
�?  �?  ├── generate-image.dto.ts         # 图片生成 DTO
�?  �?  ├── image-edit.dto.ts            # 图片编辑 DTO
�?  �?  ├── optimize-prompt.dto.ts       # 提示词优�?DTO
�?  �?  └── remove-bg.dto.ts             # 抠图 DTO
�?  ├── entities/
�?  �?  ├── ai-model.entity.ts           # AI 模型实体
�?  �?  └── ai-task.entity.ts            # AI 任务实体
�?  ├── processors/
�?  �?  ├── generate.processor.ts        # 图片生成处理�?�?  �?  ├── image-edit.processor.ts       # 图片编辑处理�?�?  �?  └── remove-bg.processor.ts      # 抠图处理�?�?  ├── services/
�?  �?  ├── ai-model.service.ts          # 模型服务
�?  �?  ├── generate.service.ts          # 生成服务
�?  �?  ├── image-edit.service.ts         # 编辑服务
�?  �?  ├── remove-bg.service.ts         # 抠图服务
�?  �?  ├── task-cleanup.service.ts     # 任务清理服务
�?  �?  └── task.service.ts              # 任务服务
�?  ├── ai.controller.ts                # AI 控制�?�?  ├── ai.module.ts                   # AI 模块
�?  └── model-scheduler.service.ts       # 模型调度服务
├── auth/                                 # 认证模块
�?  ├── auth.controller.ts              # 认证控制�?�?  ├── auth.module.ts                 # 认证模块
�?  ├── auth.service.ts                # 认证服务
�?  ├── current-user.decorator.ts      # 当前用户装饰�?�?  └── jwt-auth.guard.ts             # JWT 守卫
├── common/                               # 公共模块
�?  ├── dto/
�?  �?  └── pagination.dto.ts          # 分页 DTO
�?  ├── exceptions/
�?  �?  ├── api.exception.ts            # 自定�?API 异常
�?  �?  ├── index.ts
�?  �?  ├── network.exception.ts        # 网络异常
�?  �?  └── validation.exception.ts     # 验证异常
�?  ├── filters/
�?  �?  └── http-exception.filter.ts     # 全局异常过滤�?�?  ├── interceptors/
�?  �?  ├── logging.interceptor.ts      # 日志拦截�?�?  �?  └── transform.interceptor.ts   # 响应转换拦截�?�?  ├── utils/
�?  �?  ├── ai.utils.ts               # AI 工具函数
�?  �?  ├── date.utils.ts             # 日期工具函数
�?  �?  └── file.utils.ts             # 文件工具函数
�?  ├── cache.service.ts               # 缓存服务
�?  └── common.module.ts              # 公共模块
├── config/                               # 配置模块
�?  ├── configuration.ts               # 应用配置
�?  └── logger.config.ts              # 日志配置
├── database/                             # 数据库模�?�?  ├── seed-data.ts                 # 种子数据
�?  └── seed.service.ts               # 数据填充服务
├── material/                             # 素材模块
�?  ├── dto/
�?  �?  └── update-material.dto.ts    # 更新素材 DTO
�?  ├── entities/
�?  �?  └── material.entity.ts        # 素材实体
�?  ├── material.controller.ts        # 素材控制�?�?  ├── material.module.ts           # 素材模块
�?  └── material.service.ts          # 素材服务
├── user/                                 # 用户模块
�?  ├── dto/
�?  �?  ├── create-user.dto.ts        # 创建用户 DTO
�?  �?  └── login-user.dto.ts         # 登录用户 DTO
�?  ├── entities/
�?  �?  └── user.entity.ts           # 用户实体
�?  ├── user.controller.ts          # 用户控制�?�?  ├── user.module.ts             # 用户模块
�?  └── user.service.ts            # 用户服务
├── app.controller.ts                   # 根控制器
├── app.module.ts                      # 根模�?├── app.service.ts                     # 根服�?└── main.ts                           # 应用入口
```

---

## 5. 核心模块详解

### 5.1 Auth 模块 (认证模块)

**文件位置**: `auth/`

负责用户登录、注册、JWT Token 生成和验证�?
#### AuthService (认证服务)

```typescript
import { Injectable, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import * as bcrypt from 'bcrypt';
import { User } from '../user/entities/user.entity';
import { LoginUserDto } from '../user/dto/login-user.dto';
import { CreateUserDto } from '../user/dto/create-user.dto';

@Injectable()
export class AuthService {
  constructor(
    @InjectRepository(User)
    private userRepository: Repository<User>,
    private jwtService: JwtService,
  ) {}

  async register(dto: CreateUserDto) {
    const existing = await this.userRepository.findOne({
      where: { email: dto.email },
    });
    
    if (existing) {
      throw new UnauthorizedException('邮箱已注�?);
    }

    const hashedPassword = await bcrypt.hash(dto.password, 10);
    
    const user = this.userRepository.create({
      email: dto.email,
      password: hashedPassword,
      credits: 100, // 新用户赠�?100 积分
    });

    await this.userRepository.save(user);
    
    const token = this.jwtService.sign({ userId: user.id, email: user.email });
    
    return {
      token,
      user: {
        id: user.id,
        email: user.email,
        credits: user.credits,
      },
    };
  }

  async login(dto: LoginUserDto) {
    const user = await this.userRepository.findOne({
      where: { email: dto.email },
    });

    if (!user) {
      throw new UnauthorizedException('用户不存�?);
    }

    const valid = await bcrypt.compare(dto.password, user.password);
    
    if (!valid) {
      throw new UnauthorizedException('密码错误');
    }

    const token = this.jwtService.sign({ userId: user.id, email: user.email });

    return {
      token,
      user: {
        id: user.id,
        email: user.email,
        credits: user.credits,
      },
    };
  }
}
```

#### JwtAuthGuard (JWT 守卫)

```typescript
import { Injectable, CanActivate, ExecutionContext } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';

@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(
    private jwtService: JwtService,
    private configService: ConfigService,
  ) {}

  canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest();
    const token = this.extractTokenFromHeader(request);

    if (!token) {
      return false;
    }

    try {
      const payload = this.jwtService.verify(token, {
        secret: this.configService.get('JWT_SECRET'),
      });
      request.user = payload;
    } catch {
      return false;
    }

    return true;
  }

  private extractTokenFromHeader(request: any): string | undefined {
    const [type, token] = request.headers.authorization?.split(' ') ?? [];
    return type === 'Bearer' ? token : undefined;
  }
}
```

#### CurrentUser Decorator (当前用户装饰�?

```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';

export const CurrentUser = createParamDecorator(
  (data: unknown, ctx: ExecutionContext) => {
    const request = ctx.switchToHttp().getRequest();
    return request.user;
  },
);
```

### 5.2 AI 模块

**文件位置**: `ai/`

核心业务模块，负�?AI 任务的创建、处理和状态管理�?
#### AIController (AI 控制�?

```typescript
import { Controller, Post, Get, Param, UseGuards, Body, UseInterceptors, UploadedFile } from '@nestjs/common';
import { FileInterceptor } from '@nestjs/platform-express';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { CurrentUser } from '../auth/current-user.decorator';
import { AIService } from './services/ai.service';
import { GenerateImageDto } from './dto/generate-image.dto';
import { RemoveBgDto } from './dto/remove-bg.dto';

@Controller('ai')
@UseGuards(JwtAuthGuard)
export class AIController {
  constructor(private aiService: AIService) {}

  @Post('generate')
  async generate(
    @CurrentUser() user: any,
    @Body() dto: GenerateImageDto,
  ) {
    return this.aiService.createGenerateTask(user.userId, dto);
  }

  @Post('remove-bg')
  @UseInterceptors(FileInterceptor('file'))
  async removeBg(
    @CurrentUser() user: any,
    @UploadedFile() file: Express.Multer.File,
  ) {
    return this.aiService.createRemoveBgTask(user.userId, file);
  }

  @Get('tasks/:id')
  async getTask(@Param('id') id: number) {
    return this.aiService.getTask(id);
  }

  @Get('tasks')
  async getTasks(@CurrentUser() user: any) {
    return this.aiService.getUserTasks(user.userId);
  }
}
```

#### GenerateProcessor (图片生成处理�?

```typescript
import { Processor, Process, OnQueueActive, OnQueueProgress, OnQueueCompleted, OnQueueFailed } from '@nestjs/bull';
import { Job } from 'bull';
import { GenerateService } from '../services/generate.service';
import { TaskService } from '../services/task.service';

@Processor('ai-tasks')
export class GenerateProcessor {
  constructor(
    private generateService: GenerateService,
    private taskService: TaskService,
  ) {}

  @Process('generate')
  async handleGenerate(job: Job) {
    const { taskId, params } = job.data;
    
    await this.taskService.updateStatus(taskId, 'processing', 10);

    try {
      const result = await this.generateService.generateImage(params);
      
      await this.taskService.updateStatus(taskId, 'completed', 100, result);
      
      return result;
    } catch (error) {
      await this.taskService.updateStatus(taskId, 'failed', 0, null, error.message);
      throw error;
    }
  }

  @OnQueueActive()
  onActive(job: Job) {
    console.log(`Processing job ${job.id} of type ${job.name}`);
  }

  @OnQueueProgress()
  onProgress(job: Job, progress: number) {
    console.log(`Job ${job.id} progress: ${progress}%`);
  }
}
```

### 5.3 Material 模块 (素材模块)

**文件位置**: `material/`

负责素材文件的上传、下载、编辑和删除�?
#### MaterialService (素材服务)

```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Material } from './entities/material.entity';
import { UpdateMaterialDto } from './dto/update-material.dto';
import { fileUtils } from '../common/utils/file.utils';

@Injectable()
export class MaterialService {
  constructor(
    @InjectRepository(Material)
    private materialRepository: Repository<Material>,
  ) {}

  async upload(userId: number, file: Express.Multer.File) {
    const { filename, url, size } = await fileUtils.saveFile(file);

    const material = this.materialRepository.create({
      userId,
      name: filename,
      url,
      size,
      type: file.mimetype,
    });

    return this.materialRepository.save(material);
  }

  async findAll(userId: number) {
    return this.materialRepository.find({
      where: { userId },
      order: { createdAt: 'DESC' },
    });
  }

  async update(userId: number, id: number, dto: UpdateMaterialDto) {
    const material = await this.materialRepository.findOne({
      where: { id, userId },
    });

    if (!material) {
      throw new NotFoundException('素材不存�?);
    }

    Object.assign(material, dto);
    return this.materialRepository.save(material);
  }

  async delete(userId: number, id: number) {
    const material = await this.materialRepository.findOne({
      where: { id, userId },
    });

    if (!material) {
      throw new NotFoundException('素材不存�?);
    }

    await fileUtils.deleteFile(material.url);
    await this.materialRepository.remove(material);
  }
}
```

---

## 6. API 设计规范

### 6.1 统一响应格式

所�?API 响应统一格式，由 TransformInterceptor 自动包装�?
```typescript
{
  code: number;      // 状态码�?00 表示成功
  data: any;         // 响应数据
  message: string;   // 提示信息
}
```

#### TransformInterceptor 实现

```typescript
import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

@Injectable()
export class TransformInterceptor<T> implements NestInterceptor<T, any> {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    return next.handle().pipe(
      map(data => ({
        code: 200,
        data,
        message: 'success',
      })),
    );
  }
}
```

### 6.2 API 接口列表

#### 认证接口

| 方法 | 路径 | 描述 | 认证 |
|-----|------|------|------|
| POST | `/auth/register` | 用户注册 | �?|
| POST | `/auth/login` | 用户登录 | �?|

#### 用户接口

| 方法 | 路径 | 描述 | 认证 |
|-----|------|------|------|
| GET | `/user/profile` | 获取当前用户信息 | �?|

#### AI 接口

| 方法 | 路径 | 描述 | 认证 |
|-----|------|------|------|
| POST | `/ai/generate` | 生成图片 | �?|
| POST | `/ai/remove-bg` | 图片抠图 | �?|
| POST | `/ai/edit` | 编辑图片 | �?|
| POST | `/ai/batch` | 批量处理 | �?|
| GET | `/ai/tasks` | 获取任务列表 | �?|
| GET | `/ai/tasks/:id` | 获取任务详情 | �?|

#### 素材接口

| 方法 | 路径 | 描述 | 认证 |
|-----|------|------|------|
| GET | `/material` | 获取素材列表 | �?|
| POST | `/material/upload` | 上传素材 | �?|
| PUT | `/material/:id` | 更新素材 | �?|
| DELETE | `/material/:id` | 删除素材 | �?|

### 6.3 DTO 参数验证

使用 class-validator �?class-transformer 进行参数验证�?
```typescript
import { IsString, IsEmail, MinLength, IsOptional, IsEnum } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class LoginUserDto {
  @ApiProperty({ description: '邮箱' })
  @IsEmail({}, { message: '邮箱格式不正�? })
  email: string;

  @ApiProperty({ description: '密码' })
  @IsString()
  @MinLength(6, { message: '密码至少6�? })
  password: string;
}

export class GenerateImageDto {
  @ApiProperty({ description: '提示�? })
  @IsString()
  prompt: string;

  @ApiProperty({ description: '模型', required: false })
  @IsOptional()
  @IsString()
  model?: string;

  @ApiProperty({ description: '图片尺寸', required: false })
  @IsOptional()
  @IsString()
  size?: string;
}
```

---

## 7. 数据库设�?
### 7.1 用户�?(user)

| 字段 | 类型 | 约束 | 说明 |
|-----|------|------|------|
| id | int | PK, AI | 主键 |
| email | varchar(255) | Unique, Not Null | 邮箱 |
| password | varchar(255) | Not Null | 密码（哈希值） |
| role | varchar(50) | Default 'user' | 角色 |
| credits | int | Default 100 | 积分 |
| createdAt | timestamp | Default NOW() | 创建时间 |
| updatedAt | timestamp | Default NOW() | 更新时间 |

#### User Entity

```typescript
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn, OneToMany } from 'typeorm';
import { AITask } from '../../ai/entities/ai-task.entity';
import { Material } from '../../material/entities/material.entity';

@Entity('user')
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ unique: true })
  email: string;

  @Column()
  password: string;

  @Column({ default: 'user' })
  role: string;

  @Column({ default: 100 })
  credits: number;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;

  @OneToMany(() => AITask, task => task.user)
  tasks: AITask[];

  @OneToMany(() => Material, material => material.user)
  materials: Material[];
}
```

### 7.2 AI 任务�?(ai_task)

| 字段 | 类型 | 约束 | 说明 |
|-----|------|------|------|
| id | int | PK, AI | 主键 |
| userId | int | FK, Index | 用户 ID |
| type | varchar(50) | Index | 任务类型 |
| params | jsonb | | 任务参数 |
| status | varchar(50) | Index, Default 'pending' | 状�?|
| progress | int | Default 0 | 进度 |
| result | jsonb | Nullable | 结果 |
| error | varchar(1000) | Nullable | 错误信息 |
| createdAt | timestamp | Index, Default NOW() | 创建时间 |
| updatedAt | timestamp | Default NOW() | 更新时间 |

#### 状态枚�?
- `pending`: 等待处理
- `processing`: 处理�?- `completed`: 已完�?- `failed`: 失败

#### AITask Entity

```typescript
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn, ManyToOne, JoinColumn } from 'typeorm';
import { User } from '../../user/entities/user.entity';

export type TaskStatus = 'pending' | 'processing' | 'completed' | 'failed';
export type TaskType = 'generate' | 'remove-bg' | 'edit' | 'batch';

@Entity('ai_task')
export class AITask {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  userId: number;

  @Column({ type: 'varchar', length: 50 })
  type: TaskType;

  @Column({ type: 'jsonb' })
  params: any;

  @Column({ type: 'varchar', length: 50, default: 'pending' })
  status: TaskStatus;

  @Column({ type: 'int', default: 0 })
  progress: number;

  @Column({ type: 'jsonb', nullable: true })
  result: any;

  @Column({ type: 'varchar', length: 1000, nullable: true })
  error: string;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;

  @ManyToOne(() => User, user => user.tasks)
  @JoinColumn({ name: 'userId' })
  user: User;
}
```

### 7.3 素材�?(material)

| 字段 | 类型 | 约束 | 说明 |
|-----|------|------|------|
| id | int | PK, AI | 主键 |
| userId | int | FK, Index | 用户 ID |
| name | varchar(255) | | 素材名称 |
| url | varchar(500) | | 文件 URL |
| size | int | | 文件大小（字节） |
| type | varchar(100) | Index | 文件类型 |
| category | varchar(100) | Nullable | 分类 |
| createdAt | timestamp | Index, Default NOW() | 创建时间 |
| updatedAt | timestamp | Default NOW() | 更新时间 |

---

## 8. 安全策略

### 8.1 密码安全

- 使用 bcrypt 哈希算法
- 加盐轮数�?0
- 禁止明文存储密码

```typescript
import * as bcrypt from 'bcrypt';

// 哈希密码
const hashedPassword = await bcrypt.hash(password, 10);

// 验证密码
const isValid = await bcrypt.compare(inputPassword, hashedPassword);
```

### 8.2 JWT 安全

- 使用强密钥（至少 32 字符�?- 设置合理的过期时间（7 天）
- Token 不包含敏感信�?
```typescript
// JWT 配置
JwtModule.register({
  secret: process.env.JWT_SECRET || 'your-strong-secret-key-here',
  signOptions: { expiresIn: '7d' },
})
```

### 8.3 接口限流

使用 express-rate-limit 防止恶意请求�?
```typescript
import rateLimit from 'express-rate-limit';

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 分钟
  max: 100, // 限制 100 次请�?  message: '请求过于频繁，请稍后再试',
});

// �?main.ts 中使�?app.use(limiter);
```

### 8.4 CORS 配置

配置跨域资源共享策略�?
```typescript
app.enableCors({
  origin: process.env.ALLOWED_ORIGINS?.split(',') || '*',
  credentials: true,
});
```

### 8.5 Helmet 安全�?
使用 Helmet 中间件设置安�?HTTP 头�?
```typescript
import helmet from 'helmet';

app.use(helmet());
```

---

## 9. 异步任务处理

### 9.1 Bull 队列配置

```typescript
import { BullModule } from '@nestjs/bull';

@Module({
  imports: [
    BullModule.forRoot({
      redis: {
        host: process.env.REDIS_HOST,
        port: parseInt(process.env.REDIS_PORT || '6379'),
        password: process.env.REDIS_PASSWORD,
      },
    }),
    BullModule.registerQueue({
      name: 'ai-tasks',
    }),
  ],
})
export class AiModule {}
```

### 9.2 任务创建流程

```typescript
import { InjectQueue } from '@nestjs/bull';
import { Queue } from 'bull';

@Injectable()
export class AIService {
  constructor(
    @InjectQueue('ai-tasks') private aiQueue: Queue,
    private taskService: TaskService,
  ) {}

  async createGenerateTask(userId: number, params: any) {
    const task = await this.taskService.create({
      userId,
      type: 'generate',
      params,
      status: 'pending',
    });

    await this.aiQueue.add('generate', {
      taskId: task.id,
      params,
    }, {
      priority: 1,
      attempts: 3,
      backoff: {
        type: 'exponential',
        delay: 1000,
      },
    });

    return { taskId: task.id };
  }
}
```

---

## 10. 日志系统

### 10.1 Winston 配置

```typescript
import winston from 'winston';
import 'winston-daily-rotate-file';

const logFormat = winston.format.combine(
  winston.format.timestamp(),
  winston.format.json(),
);

export const winstonConfig = {
  transports: [
    new winston.transports.Console({
      format: winston.format.combine(
        winston.format.colorize(),
        winston.format.simple(),
      ),
    }),
    new winston.transports.DailyRotateFile({
      dirname: 'logs',
      filename: 'application-%DATE%.log',
      datePattern: 'YYYY-MM-DD',
      maxSize: '20m',
      maxFiles: '14d',
      format: logFormat,
    }),
  ],
};
```

### 10.2 日志级别

从低到高�?- `debug`: 调试信息
- `info`: 一般信�?- `warn`: 警告
- `error`: 错误

### 10.3 Nest-Winston 集成

```typescript
import { WinstonModule } from 'nest-winston';

@Module({
  imports: [
    WinstonModule.forRoot(winstonConfig),
  ],
})
export class AppModule {}
```

---

## 11. 开发规�?
### 11.1 代码风格

- 使用 TypeScript 严格模式
- 使用 ES6+ 语法
- 使用 async/await 处理异步
- 遵循 NestJS 官方风格指南

### 11.2 模块结构规范

每个模块应包含：
- `*.module.ts`: 模块定义
- `*.controller.ts`: 控制�?- `*.service.ts`: 服务
- `dto/*.dto.ts`: 数据传输对象
- `entities/*.entity.ts`: 数据库实�?
### 11.3 Git 提交规范

- `feat:` 新功�?- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 重构
- `test:` 测试相关
- `chore:` 构建/工具相关

---

## 附录

### A. 本地开发环境搭�?
```bash
# 安装依赖
cd server
pnpm install

# 复制环境变量模板
cp .env.example .env
# 编辑 .env 填入配置

# 使用 Docker Compose 启动数据库和 Redis
docker-compose up -d

# 启动开发服务器
pnpm start:dev

# 访问 API 文档
http://localhost:3000/api
```

### B. 环境变量配置

```env
# 服务配置
PORT=3000
NODE_ENV=development

# 数据库配�?DATABASE_URL=postgresql://user:password@localhost:5432/ai_material
DATABASE_SSL=false

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT 配置
JWT_SECRET=your-strong-secret-key-here-change-in-production
JWT_EXPIRES_IN=7d

# AI API 配置
TONGYI_API_KEY=your-tongyi-api-key
DOUBAI_API_KEY=your-doubao-api-key
REMOVE_BG_API_KEY=your-remove-bg-api-key

# 日志配置
LOG_LEVEL=info
```

### C. 相关资源链接

- [NestJS 文档](https://docs.nestjs.com)
- [TypeORM 文档](https://typeorm.io)
- [Bull 文档](https://docs.bullmq.io)
- [PostgreSQL 文档](https://www.postgresql.org/docs)
- [Redis 文档](https://redis.io/docs)
- [Swagger 文档](https://swagger.io/docs)
